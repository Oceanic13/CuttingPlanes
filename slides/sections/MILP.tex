\section{Mixed Integer Linear Program}

\begin{frame}{Mixed Integer Linear Program and Linear Program Relaxation}
%Some of the problem variables are restricted to be integer.
\small
\begin{columns}

	\column{0.5\textwidth}
	\begin{figure}
	\centering
	\includesvg[width=0.3\textwidth]{imgs/milp.svg}
	\end{figure}
	
	\begin{tcolorbox}[colback=white, title={MILP (standard form)}]
    \begin{align*}
    	\min_{x}\quad &c^\top x \\
    	\text{s.t.}\quad & x\in F \\
    	:= &\{x \:\vert\: Ax \leq b, x \in \mathbb{Z}_{\geq 0}^{n_1} \times \mathbb{R}_{\geq 0}^{n-n_1} \}
    \end{align*}
    \end{tcolorbox}

	\column{0.5\textwidth}    
	\begin{figure}
	\centering
	\includesvg[width=0.3\textwidth]{imgs/lp.svg}
	\end{figure}
	
	\begin{tcolorbox}[colback=white, title={LP Relaxation}]
    \begin{align*}
    	\min_{x}\quad &c^\top x \\
    	\text{s.t.}\quad &x \in P\\
    	:= &\{x \:\vert\: Ax \leq b, x \in \mathbb{R}_{\geq 0}^{n}\}
    \end{align*}
    \end{tcolorbox}
\end{columns}
$c,x \in \mathbb{R}^n, A \in \mathbb{R}^{m \times n}, b \in \mathbb{R}^m$
\end{frame}
%TODO Illustration
% Mention Transformations: =, nonnegativity, slack variables, ... -> Transform to standard form



\begin{frame}{Notation}
%TODO % Notation: P (for polyhedron) is the feasible region of the relaxation, F = P \cap (\mathbb{Z}^{n_1} \times \mathbb{R}^{n-n_1}) is the feasible region of the milp. Note that conx(F) is again a polyhedron but computing conv(F) is expensive. first n_1 vars are integer constrained

\begin{itemize}
\item $x_1,...,x_{n_1}:$ Integer variables
\item $x_{n_1},...,x_n:$ Real variables
\item $F, x^*:$ Feasible region and optimal solution of the MILP
\item $P, \hat{x}:$ Feasible region and optimal solution of the LP-relaxation ($F = P \cap \mathbb{Z}_{\geq 0}^{n_1} \times \mathbb{R}_{\geq 0}^{n-n_1}$)
\end{itemize}

\begin{itemize}
\item $n_1 = 0 \Rightarrow \text{Linear Program (LP)}$
\item $n_1 = n \Rightarrow \text{Integer Linear Program (ILP)}$
\item $x_i \in \{0,1\}\Rightarrow \text{(Mixed) Binary Program ((M)BP)}$
%TODO Applications
\end{itemize}

\end{frame}

\begin{frame}{Observations}
% F, x^* feasible region and solution of MILP. P, \hat{x}: feasible region and solution of relaxation
\begin{columns}
\column{0.4\textwidth}
\begin{figure}[p]
        \centering
        \includesvg[]{imgs/naive_rounding.svg}
    \end{figure}

\column{0.6\textwidth}
\begin{itemize}[<+->]
\item $\hat{x}$ can be found at a vertex of $P$\\
(Simplex Algorithm).
% Assuming Bounded!

\item In general, $\text{round}(\hat{x}) \neq x^*$

%\item The relaxed solution gives a lower bound: $c^\top \hat{x} \leq c^\top x^*$. Not relevant here

\item If $\hat{x}$ is already feasible,\\
then $c^\top \hat{x} = c^\top x^*$.
%meaning the relaxation is equivelent to the original MILP.


\end{itemize}
\end{columns}
	
\end{frame}